import 'package:flutter/material.dart';

class MapScrean extends StatelessWidget {
  const MapScrean({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Map')));
  }
}
